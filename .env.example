# SwissIsoform Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# ============================================================================
# API Credentials
# ============================================================================

# NCBI E-utilities API Key (for ClinVar)
# Register for a free API key at: https://www.ncbi.nlm.nih.gov/account/
# Without an API key, requests are limited to 3 per second
# With an API key, the limit increases to 10 per second
NCBI_API_KEY=your_ncbi_api_key_here

# ============================================================================
# API Endpoints
# ============================================================================

# gnomAD API Endpoint (no API key needed)
# Rate limited to 10 requests per IP per 60 seconds
# Default: https://gnomad.broadinstitute.org/api
GNOMAD_API_URL=https://gnomad.broadinstitute.org/api

# ClinVar/NCBI E-utilities Base URL
# Default: https://eutils.ncbi.nlm.nih.gov/entrez/eutils
CLINVAR_BASE_URL=https://eutils.ncbi.nlm.nih.gov/entrez/eutils

# ============================================================================
# API Reliability Settings
# ============================================================================

# gnomAD request timeout in seconds (default: 30)
GNOMAD_TIMEOUT=30

# Number of retry attempts for failed gnomAD requests (default: 3)
GNOMAD_MAX_RETRIES=3

# Initial retry delay in seconds, doubles with each retry (default: 1.0)
# Retry delays: 1s, 2s, 4s
GNOMAD_RETRY_DELAY=1.0
